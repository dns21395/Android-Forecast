apply from: "$rootDir/common.gradle"
apply plugin: 'kotlin-kapt'

dependencies {
    implementation(
            project(':core:redux'),
            project(':core:store'),

            deps.dagger.dagger,
    )

    kapt(deps.dagger.compiler)
}

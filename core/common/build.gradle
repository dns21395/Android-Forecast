apply from: "$rootDir/common.gradle"

dependencies {
    implementation(
            project(':core:data'),
            project(':core:redux'),
            project(':core:store'),

            deps.coroutines.coroutines,
            deps.lifecycle.viewmodel,
            deps.modo.modo,
    )
}

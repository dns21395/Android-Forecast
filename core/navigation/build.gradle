apply from: "$rootDir/common.gradle"

dependencies {
    implementation(
            project(':core:data'),
            project(':core:di'),

            deps.coroutines.coroutines,
            deps.dagger.dagger,
            deps.lifecycle.viewmodel,
            deps.modo.modo,
    )
}
